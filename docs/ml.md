# ML-система

## Архитектура ансамбля

Для каждого тикера обучается отдельный `VotingRegressor` — усреднение предсказаний:

```python
VotingRegressor(
    estimators=[
        ("lgbm", LGBMRegressor(objective="regression_l1", ...)),
        ("et", ExtraTreesRegressor(...)),
    ],
    n_jobs=1,  # последовательный фит базовых моделей — меньше RAM на слабом сервере
)
```

`StandardScaler` не используется: деревья инвариантны к монотонным преобразованиям признаков.

**Почему регрессия, а не классификация:** классификатор (BUY/SELL/HOLD) дискретизирует непрерывный P&L через
произвольный порог, создаёт дисбаланс классов (~72% HOLD) и требует `class_weight="balanced"`.
Регрессор напрямую предсказывает ожидаемый net P&L, устраняя рассогласование между целью обучения и метрикой HPO.

**Почему `regression_l1` (MAE) для LightGBM:** P&L содержит выбросы — SL-хиты (−3%) и TP-хиты (+1.5%).
MAE линейно штрафует отклонения, тогда как MSE (`regression`) квадратично раздувает их вклад,
смещая модель к нейтральным предсказаниям вблизи среднего.

**Почему ExtraTrees, а не RandomForest:** ET использует случайные пороги разбиений вместо
поиска лучшего — корреляция с LightGBM значительно ниже, ансамбль получает реальное разнообразие.

Весь ансамбль сохраняется в одном `.pkl` файле.

> **Важно:** после изменения набора признаков (`features.py`) или структуры ансамбля
> необходимо запустить `python -m scripts.train_model --force-tune` —
> Optuna подберёт гиперпараметры под новый набор признаков.

## Per-ticker отбор признаков

Обучение проходит в **два прохода**:

1. **Проход 1** — быстрый `fit` ансамбля на всех 52 признаках. Вычисляется нормализованная
   importance для каждой модели (LightGBM и ExtraTrees независимо нормализуются к сумме = 1,
   затем усредняются). Признаки с
   `avg_importance < ML_FEATURE_IMPORTANCE_THRESHOLD` отбрасываются.

2. **Проход 2** — оптимизация порога уверенности (Optuna, без утечки) и финальный `fit` только на отобранных признаках.
   Список признаков сохраняется в `features_{ticker}_{version}.json`.

Каждый тикер получает **свой** набор признаков:

```
features_SBER_v2.json  → ["OBV", "cmf_20", "vwap_ratio", ...]   # 32 признака
features_LKOH_v2.json  → ["high_252_ratio", "MACD_hist", ...]   # 40 признаков
features_YDEX_v2.json  → ["hist_vol_20", "donchian_pct", ...]   # 37 признаков
```

Инференс загружает `features_{ticker}_{version}.json` автоматически — изменений в коде не требуется.

## Признаки (52 штуки)

Все признаки нормализованы — не зависят от абсолютного уровня цены тикера.
Это обеспечивает стационарность и корректное дообучение при росте или падении цены со временем.

| Группа | Признак | Описание |
|---|---|---|
| Тренд | `SMA_20`, `SMA_50` | Скользящие средние |
| Тренд | `ema12_ratio`, `ema26_ratio` | `close / EMA` — цена выше EMA > 1 (бычий), ниже < 1 |
| Тренд | `ADX_14` | Сила тренда (0–100) |
| Импульс | `RSI_14` | Осциллятор перекупленности/перепроданности |
| Импульс | `MACD`, `MACD_signal`, `MACD_hist` | MACD-индикатор |
| Импульс | `ROC_10` | Темп изменения цены за 10 свечей |
| Импульс | `MFI_14` | Money Flow Index — RSI с учётом объёма [0–100] |
| Импульс | `stoch_k`, `stoch_d` | Stochastic медленный %K/%D — позиция цены в диапазоне [0–100] |
| Импульс | `CCI_14` | Commodity Channel Index — отклонение цены от статистической нормы |
| Импульс | `aroon_up`, `aroon_down` | Сколько баров назад был max/min — сила и направление тренда |
| Импульс (дельты) | `rsi_delta_4h` | RSI сейчас − RSI 4 бара назад — направление осциллятора |
| Импульс (дельты) | `stoch_k_delta_4h` | Stochastic %K сейчас − 4 бара назад |
| Импульс (дельты) | `macd_hist_delta_4h` | MACD histogram сейчас − 4 бара назад — усиление/ослабление |
| Волатильность | `bb_pct_b` | Bollinger %B: позиция в полосах [0 = нижняя, 1 = верхняя] |
| Волатильность | `atr_ratio` | `ATR_14 / close` — волатильность как % от цены |
| Волатильность | `BB_width` | `(BB_upper − BB_lower) / BB_mid` — ширина полос |
| Волатильность | `hist_vol_20` | Скользящее std доходностей за 20 баров — историческая волатильность |
| Объём | `OBV` | On-Balance Volume |
| Объём | `VOLUME_SMA_20` | Скользящее среднее объёма |
| Объём | `volume_ratio` | `volume / VOLUME_SMA_20` — повышенный объём > 1 |
| Объём | `cmf_20` | Chaikin Money Flow: покупательское давление по объёму за 20 баров |
| Объём | `volume_change_1h`, `volume_change_4h` | Изменение объёма за 1/4 бара — предвестник ценового движения |
| Производные | `close_sma20_ratio`, `close_sma50_ratio` | Цена относительно SMA |
| Производные | `high_low_ratio` | `(high − low) / close` — диапазон бара |
| Производные | `sma20_sma50_ratio` | > 1 = золотой крест (бычий), < 1 = мёртвый крест |
| Производные | `vwap_ratio` | `close / VWAP` — цена относительно объёмно-взвешенной средней дня |
| Экстремумы | `high_252_ratio` | `close / max(close, 252 бара)` — позиция у 52-недельного максимума |
| Экстремумы | `low_252_ratio` | `close / min(close, 252 бара)` — позиция у 52-недельного минимума |
| Donchian | `donchian_pct` | Позиция цены в 20-барном диапазоне: 1.0 = пробой вверх, 0.0 = вниз |
| Donchian | `donchian_width` | Ширина Donchian канала / close — режим рынка (сжатие vs расширение) |
| Роллинг | `return_mean_8h` | Среднее доходностей за 8 баров — краткосрочный тренд |
| Роллинг | `return_std_8h` | Std доходностей за 8 баров — мини-волатильность |
| Структура свечи | `body_ratio` | `(close − open) / (high − low)` — > 0 бычья, < 0 медвежья |
| Структура свечи | `upper_shadow`, `lower_shadow` | Размер верхней/нижней тени — отверженные уровни |
| Структура свечи | `gap` | `(open − prev_close) / prev_close` — гэп при открытии |
| Лаговые доходности | `return_1h`, `return_4h`, `return_8h`, `return_24h` | `pct_change(N)` — прямой сигнал импульса; один из сильнейших предикторов |
| Временные | `hour_sin`, `hour_cos` | Циклическое кодирование часа МСК (23:00 и 00:00 — соседние точки) |
| Временные | `dayofweek_sin`, `dayofweek_cos` | Циклическое кодирование дня недели (0=Пн, 4=Пт) |

## Метрика оптимизации (Sortino ratio)

Вместо F1-macro Optuna HPO и CV-оценка ансамбля используют **Sortino ratio P&L-симуляции**:

```
Sortino = mean(P&L) / downside_std(P&L)
```

где `downside_std` — стандартное отклонение только убыточных сделок.
В отличие от Sharpe не штрафует за случайный крупный выигрыш: высокая прибыль не увеличивает знаменатель.
Для торговой системы с ограниченными потерями (SL) и несимметричными выигрышами (TP) теоретически корректнее Sharpe.

**Симуляция сделок** (`_simulate_pnl`):
- Одна позиция за раз (нет перекрытия позиций)
- Вход: `close[t]` — цена закрытия свечи с BUY-сигналом (`pnl_pred >= threshold`)
- Выход по приоритету: 1) SL, 2) TP, 3) SELL-сигнал модели **если позиция прибыльна**, 4) `close[t + lookahead]`
- После фактического выхода на баре `j` следующая сделка доступна с бара `j+1` (не конец окна)
- P&L сделки — точная реплика торговой логики планировщика:
  ```
  gross      = (exit - entry) / entry
  commission = 2 × commission_pct
  tax        = max(0, (gross - commission) × tax_pct)   # НДФЛ только на прибыль
  net        = gross - commission - tax
  ```

**Сигналы из регрессора:**
```python
pnl_pred = model.predict(X_val)          # предсказанный net P&L
y_pred   = np.where(pnl_pred >= 0, 2, 1) # BUY если ожидаем прибыль
y_sell   = (pnl_pred < 0).astype(int8)   # SELL-выход если ожидаем убыток
```
SELL-выход происходит только когда позиция прибыльна (зеркалит `scheduler.py`).

**Преимущества перед F1:**
- Напрямую моделирует торговую прибыльность, а не качество классификации
- Учитывает НДФЛ — симуляция калибруется к реальной доходности, не к завышенной
- Штрафует только убыточную волатильность (downside), не трогает прибыльную
- Штрафует "молчаливые" модели: при < `ML_SHARPE_MIN_TRADES` сделок → возвращает 0.0

**Оптимизация порога входа per-ticker** выполняется **до** финального фита:
временный ансамбль обучается на 80% данных, порог `[0.0, 0.02]` подбирается Optuna на holdout 20% — без утечки.
Затем финальный ансамбль переобучается на 100% данных.

## Целевые P&L значения (compute_pnl_targets)

Вместо дискретных меток BUY/SELL/HOLD регрессор обучается на непрерывном **net P&L** каждого бара
(`compute_pnl_targets` в `ml/labels.py`):

```python
# Для каждого бара: симулируем BUY → выход по SL/TP/lookahead → считаем net P&L
entry      = close_window[:, 0]
# SL/TP триггеры в net-базисе (совпадают с adjusted_sl/tp_price в profitability.py):
sl_prices  = entry * (1 + c - sl_pct) / (1 - c)
tp_prices  = entry * ((1 + c) + tp_pct / (1 - t)) / (1 - c)
# P&L после комиссии и НДФЛ:
gross      = (exit_price - entry) / entry
net_pnl    = gross - 2*commission - max(0, (gross - 2*commission) * tax_pct)
```

**Что это даёт:**
- Нет дискретизации: непрерывный P&L полностью описывает доходность сделки
- Нет дисбаланса классов: регрессия не требует `class_weight="balanced"`
- Нет `ML_THRESHOLD` для меток: порог входа теперь оптимизируется в P&L-пространстве per-ticker
- Полное выравнивание между целью обучения и метрикой HPO (Sortino P&L-симуляции)

`ML_THRESHOLD` в `.env` — fallback порог входа (предсказанный net P&L) для тикеров без `best_threshold_*.json`.

## Optuna HPO — настройки самплера и пруннера

Каждый раз при полном обучении (`--force-tune` или первый запуск) Optuna подбирает гиперпараметры двух моделей.

### Самплер (TPESampler)

Байесовская оптимизация через Tree-structured Parzen Estimator. Первые `n_startup_trials` трайлов — случайный поиск,
после чего TPE строит суррогатную модель и предлагает перспективные точки.

| Модель | `n_startup_trials` | Итераций (`ML_OPTUNA_TRIALS_*`) |
|---|---|---|
| LightGBM | 20 (~2-3× кол-во параметров, 9 параметров) | `ML_OPTUNA_TRIALS_LGBM` = 75 |
| ExtraTrees | 15 (~2-3× кол-во параметров, 6 параметров) | `ML_OPTUNA_TRIALS_ET` = 40 |
| Порог уверенности | 5 (1D задача) | `ML_THRESHOLD_N_TRIALS` = 50 |

### Пруннер (MedianPruner)

Досрочно останавливает трайлы, чьё промежуточное значение Sortino хуже медианы завершённых трайлов на том же шаге CV.
Значение после каждого фолда передаётся через `trial.report(score, step=fold_idx)`.

| Параметр | Значение | Смысл |
|---|---|---|
| `n_startup_trials` | 5 | Пруннер не активен для первых 5 трайлов (нет медианы для сравнения) |
| `n_warmup_steps` | 2 | Первые 2 фолда CV всегда выполняются — Sortino накапливает достаточно сделок |

### Гиперпараметры LightGBM

| Параметр | Диапазон | Смысл |
|---|---|---|
| `num_leaves` | 16–255 | Сложность дерева; log |
| `max_depth` | 4–15 | Максимальная глубина |
| `learning_rate` | 0.01–0.3 | Шаг градиентного спуска; log |
| `n_estimators` | 100–1000 | Число деревьев |
| `subsample` | 0.6–1.0 | Доля строк на каждое дерево (bagging) |
| `subsample_freq` | 1 (фиксировано) | Активирует bagging; без него `subsample` игнорируется |
| `colsample_bytree` | 0.5–1.0 | Доля признаков на дерево |
| `min_child_samples` | 10–100 | Мин. объектов в листе |
| `reg_alpha` | 0.0–1.0 | L1-регуляризация листьев |
| `reg_lambda` | 0.0–5.0 | L2-регуляризация листьев |
| `min_split_gain` | 0.0–0.5 | Мин. прирост для разбиения узла |

### Гиперпараметры ExtraTreesRegressor

| Параметр | Диапазон | Смысл |
|---|---|---|
| `n_estimators` | 100–500 | Число деревьев |
| `max_depth` | 5–18 | Максимальная глубина (log2(8000)≈13 — выше бесполезно) |
| `min_samples_split` | 2–20 | Мин. объектов для разбиения |
| `min_samples_leaf` | 1–10 | Мин. объектов в листе |
| `max_features` | 0.3–1.0 | Доля признаков на каждое разбиение |
| `min_impurity_decrease` | 0.0–0.01 | Мин. снижение примеси для разбиения |

## Ночное дообучение

Каждую ночь в `RETRAIN_HOUR:RETRAIN_MINUTE` (по `RETRAIN_TIMEZONE`) бот автоматически:

1. **Собирает новые свечи** — инкрементально, только с момента последней записи в БД
2. **Переобучает per-ticker ансамбли** — Optuna HPO пропускается (используется кеш `best_params_*.json`).
   CV запускается с `n_splits=3` — метрика Sortino обновляется каждую ночь.
3. **Сбрасывает in-memory кеш** — следующий сигнал берётся из обновлённых весов
4. **Отправляет уведомление в Telegram** — список обученных тикеров и ошибок

Swap-thrashing устранён снижением `n_splits` до 3 и `n_jobs=1` везде.

**Оптимизация порога уверенности per-ticker:** после финального фита Optuna подбирает
`confidence_threshold` индивидуально для каждого тикера (50 трайлов, последние 20% данных).
Критерий — Sortino ratio P&L-симуляции: `_sortino_score(_simulate_pnl(...))`.
При менее `ML_SHARPE_MIN_TRADES` сделок возвращается 0.0 — штраф "молчаливым" моделям.
Результат сохраняется в `ml/weights/best_threshold_{ticker}_{version}.json`.
Scheduler загружает его вместо глобального `TRADING_CONFIDENCE_THRESHOLD`.
Настраивается через `ML_THRESHOLD_N_TRIALS` и `ML_SHARPE_MIN_TRADES`.

Настройки: `RETRAIN_ENABLED`, `RETRAIN_HOUR`, `RETRAIN_MINUTE`, `RETRAIN_TIMEZONE`, `RETRAIN_FORCE_TUNE` — см. [settings.md](settings.md#ночное-дообучение).

## Оптимизация памяти при обучении

Тикеры загружаются **последовательно** — по одному из PostgreSQL. В RAM одновременно
находятся данные только одного тикера. После завершения тикера память освобождается явно:

```python
del probe      # ансамбль прохода 1 после отбора признаков
del group      # свечи тикера после обучения
gc.collect()   # принудительный сбор
```

Кросс-валидация запускается с `n_jobs=1` — без форка процессов и копий X в RAM.
`VotingRegressor(n_jobs=1)` — базовые модели обучаются последовательно, без параллельных fork.
`ExtraTreesRegressor(n_jobs=1)` — однопоточно, без внутренней параллелизации.

Ночное переобучение включает CV (n_splits=3) для актуализации метрик Sortino.
Optuna HPO пропускается — параметры берутся из кеша. Время обучения ~10–15 мин на 10 тикеров.

Это позволяет обучать все 10 тикеров на сервере с ограниченной оперативной памятью (2GB RAM).

## LRU-кеш моделей при инференсе

10 pkl-файлов моделей весят суммарно ~670 MB на диске и разворачиваются в ~1.1 GB RAM
при одновременной загрузке. На сервере с 2 GB это вызывает swap-thrashing.

`ml/predict.py` использует **LRU-кеш** (`collections.OrderedDict`) с лимитом
`ML_MODEL_CACHE_SIZE` (по умолчанию `1`). Тикеры обрабатываются поочерёдно —
достаточно держать одну модель в RAM. При загрузке новой модели старая вытесняется:

```
predict_all([SBER, GAZP, ..., YDEX]) — последовательно:
  загрузить SBER  → сигнал → вытеснить SBER
  загрузить GAZP  → сигнал → вытеснить GAZP
  ...
Пик RAM: ~100–130 MB (1 модель) вместо 1.1 GB
```

Overhead на инференс: ~1–2 с на тикер (чтение pkl с диска) — незначим для часового цикла.

| `ML_MODEL_CACHE_SIZE` | RAM на модели | Когда использовать |
|---|---|---|
| `1` | ~100 MB | сервер 2 GB (рекомендуется) |
| `3` | ~300 MB | сервер 4+ GB |
| `10` | ~1.1 GB | сервер 8+ GB |

## Визуализация обучения

При запуске `python -m scripts.train_model` прогресс отображается в терминале:

```
========================================================
  [1/10] SBER  (21480 строк)
========================================================
    LightGBM HPO   [из кеша]
    ExtraTrees HPO [из кеша]
    Отбор признаков [из кеша]
    CV оценка   [пропущена — skip_cv]          ← при ночном запуске
  > Финальное обучение ансамбля... ok
  Сохранено: ensemble_SBER_v2.pkl

========================================================
  ИТОГ: 10/10 тикеров обучено
    + SBER     -> ensemble_SBER_v2.pkl
    ...
    + MTSS     -> ensemble_MTSS_v2.pkl
========================================================
```

Чтобы увидеть таблицу важности признаков после каждого тикера — установить `ML_PRINT_FEATURE_IMPORTANCE=true` в `.env`.

Гиперпараметры кешируются в `ml/weights/best_params_{model}_{ticker}_{version}.json`.
При повторном запуске без `--force-tune` HPO пропускается — используется кеш.

## Последние результаты обучения

Обновляются автоматически в `ml/weights/last_results.json` после каждого обучения.
Метрика: **Sortino ratio** P&L-симуляции на CV.

Старые результаты (до марта 2026) недействительны — архитектура пересмотрена:
- **Март 2026 v1**: классификатор (BUY/SELL/HOLD) → Sortino HPO, симуляционные метки по gross
- **Март 2026 v2**: регрессор net P&L → Sortino HPO, `compute_pnl_targets`, нет class_weight,
  SELL-выход только при прибыльной позиции, `next_available = i + exit_j + 1`
